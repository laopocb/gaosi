# 代码上传 GitHub 说明（laopocb/gaosi）

一键上传当前代码到 https://github.com/laopocb/gaosi ，**所有 .ply / .sog 数据不会上传**（.gitignore 已排除，且上传走 GitHub API 重建孤儿提交，历史中也无数据）。

## 一键上传

在项目根目录（`D:\lm\高斯`）执行：

```bash
python scripts/upload-to-github.py
```

脚本自动完成：读取 token → 收集代码文件 → 上传 blob → 重建 main 分支 → 输出上传结果。
**无需手动传 token、无需 git push。**

## 脚本做了什么

1. **读 token**：自动从 Windows 凭据管理器读取（`git-credential-manager get --no-ui`，无弹窗）。如果凭据丢失，手动传参：`python scripts/upload-to-github.py <TOKEN>`
2. **收集文件**：`git ls-files` 列出全部被 git 追踪的文件（数据目录已被 .gitignore 排除，不会进包）
3. **上传**：base64 blob → 构建 tree → 创建**孤儿提交**（无父提交，历史干净无数据）→ 强制更新 `refs/heads/main`
4. **验证**：输出 `tree sha` / `commit sha` / 成功提示

## 上传前记得

- 修改了代码后：先 `git add <文件>` + `git commit`，让脚本能收集到最新文件
  （脚本上传的是**当前工作区文件内容**，但文件清单来自 `git ls-files`，新文件需先 `git add`）
- 新增了数据文件：确认已加入 `.gitignore`（`*.ply` / `*.sog` / `sog_data/` / `sog_data_mobile/` / `new_data/` / `deploy-package/` 等）

## 为什么不用 git push

- github.com 主站在当前网络下被代理阻断（TLS 握手失败），`git push` 走不通
- 但 api.github.com 稳定可用，所以脚本走 GitHub API 上传（socks5h 代理 127.0.0.1:5180）

## 常见问题

| 现象 | 处理 |
|---|---|
| `未找到 GitHub token` | 重新登录 GitHub → Settings → Developer settings → Personal access tokens，创建 token（repo 权限）；或手动传参 |
| 某文件 `[失败]` | 检查是否超 100MB（GitHub 单文件上限），数据文件不要传 |
| 想验证远程内容 | 浏览器打开 https://github.com/laopocb/gaosi 查看 |

## 需要手工上传数据时

数据（ply/sog）不进 git，如需部署到服务器，用 `deploy-package/` 目录（内有完整数据与 Docker 配置），按其中「部署说明.md」操作。
